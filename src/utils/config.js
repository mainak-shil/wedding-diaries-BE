const ATTRIBUTE_FILTER_TYPES = {
  TEXT: 'Text',
  MIN_MAX_RANGE: '[minValue,maxValue]',
  LAT_LONG: '[Lat,Lng]',
};

const KM_RANGE_SEARCH = 10;

module.exports = { ATTRIBUTE_FILTER_TYPES, KM_RANGE_SEARCH };
