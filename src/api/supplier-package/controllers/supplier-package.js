'use strict';

/**
 * supplier-package controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::supplier-package.supplier-package');
